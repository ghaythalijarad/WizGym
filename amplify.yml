version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step required — static files only"
  artifacts:
    baseDirectory: frontend/admin-dashboard
    files:
      - '**/*'
  cache:
    paths: []
